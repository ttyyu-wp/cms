<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.lx.cms.mapper.PictureMapper">

    <select id="getMaxContactId" resultType="java.lang.String">
        SELECT MAX(ct_id) FROM t_contact
    </select>
</mapper>
